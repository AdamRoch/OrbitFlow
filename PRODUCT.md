# OrbitFlow product contract

Accepted scope from Adam, 2026-09-06: implement every mandatory requirement in agent-factory-brief.md. The central demo builds a working browser application with local persistence, then improves it through a real Telegram conversation. Retain source, revisions, preview, review feedback, and human approval. Provide editable build and improve templates on a generic workflow graph. Authentication, payments, arbitrary application backends, and public deployment are outside the initial generated-app scope.

The original repository is read-only. Local isolated services and commits are authorized. Paid application provider calls require a bounded budget. No remote publishing, deployment, migration, old bot switch, or deletion of existing work is authorized.
